name: Release production image

on:
  release:
    types: [published]

permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/deeplearnphysics/dlpgen-opt
  CACHE_IMAGE: ghcr.io/deeplearnphysics/dlpgen-opt-buildcache:buildcache

concurrency:
  group: release-image
  cancel-in-progress: false

jobs:
  build-and-push:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out release sources
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.release.tag_name }}
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Select immutable and rolling tags
        id: image-tags
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_ID: ${{ github.event.release.id }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          if [[ ! "${RELEASE_TAG}" =~ ^v([0-9]+\.[0-9]+\.[0-9]+)$ ]]; then
            echo "Release tag must use vX.Y.Z syntax, got: ${RELEASE_TAG}" >&2
            exit 1
          fi
          image_tag="${BASH_REMATCH[1]}"
          latest_id="$(gh api "repos/${GITHUB_REPOSITORY}/releases/latest" --jq .id)"
          {
            echo 'tags<<EOF'
            echo "${IMAGE}:${image_tag}"
            if [[ "${RELEASE_ID}" == "${latest_id}" ]]; then
              echo "${IMAGE}:latest"
            fi
            echo 'EOF'
          } >> "${GITHUB_OUTPUT}"

      - name: Build and push the release image
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ steps.image-tags.outputs.tags }}
          pull: true
          cache-from: |
            type=registry,ref=${{ env.CACHE_IMAGE }}
            type=gha,scope=production-amd64
          cache-to: |
            type=registry,ref=${{ env.CACHE_IMAGE }},mode=max
            type=gha,mode=max,scope=production-amd64
          provenance: mode=max
          sbom: true

      - name: Verify published image manifests
        env:
          TAGS: ${{ steps.image-tags.outputs.tags }}
        run: .github/scripts/verify-container-image.sh "${IMAGE}" ${TAGS}
